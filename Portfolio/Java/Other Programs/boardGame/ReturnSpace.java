
public class ReturnSpace extends Space
{

	/**
	 * @param name
	 * @param location
	 * @param returnSpace
	 */
	public ReturnSpace(String name, Location location) 
	{
		super(name, location);
	}
	
	public boolean equals(ReturnSpace other)
	{
		return super.equals(other);
	}
}
