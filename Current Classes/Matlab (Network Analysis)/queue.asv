function [serve] = queue(server, queueSize, serverQueue)
%QUEUE Summary of this function goes here
%   Detailed explanation goes here

if length(serverQueue) > queueSize
% Block
else
packet = 
server1Queue(length(server1Queue) + 1) = packet
end

